spring:
  application:
    name: user-service
  cloud:
    config:
      uri:
      - http://localhost:9000
#Eureka 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
task-service:
  ribbon:
    listOfServers: http://localhost:8083/ ,http://localhost:8085    
