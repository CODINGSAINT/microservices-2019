spring:
  application:
    name: task-service
  cloud:
    config:
      uri:
      - http://localhost:9000
#Eureka 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/